/**
 * 
 * Trie - prefix tree, structures additive prefix structure
 * 
 * Imagine every word in the dictionary with the nodes as letters connecting to form a word
 * as you move down a tree.
 * 
 */

class TrieNode {
    constructor(val){
        this.val = val;
    }
}